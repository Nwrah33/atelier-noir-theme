{#
    ملخص سريع للسياسات يظهر أسفل زر الإضافة للسلة في صفحة المنتج
    يسحب من نفس نصوص السياسات المُدخلة في إعدادات الثيم
#}
<div class="policies-summary">
    <a href="/policies/shipping" class="policies-summary__item">
        <span class="policies-summary__icon">🚚</span>
        <span>{{ theme.settings.get('policy_shipping_title')|default('الشحن والتوصيل') }}</span>
    </a>
    <a href="/policies/returns" class="policies-summary__item">
        <span class="policies-summary__icon">↺</span>
        <span>{{ theme.settings.get('policy_returns_title')|default('الإرجاع والاستبدال') }}</span>
    </a>
</div>

<style>
.policies-summary {
    display: flex;
    gap: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-border);
    margin-top: 1.5rem;
}
.policies-summary__item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.85rem;
    color: var(--color-text-muted);
    transition: color var(--duration-fast) var(--ease-luxury);
}
.policies-summary__item:hover {
    color: var(--color-primary);
}
</style>
